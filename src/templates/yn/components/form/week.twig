{% extends "yn/components/form/basic.twig" %}

{% block field %}
	<input class="yn-date flatpickr" placeholder="Bitte wählen…" id="{{ form.uid }}_{{ field.alias }}" type="{{ field.type }}" name="fields{{ parent }}[{{ field.alias }}]" {% if field.placeholder %}placeholder="{{ field.placeholder }}"{% endif %} {% if field.required %}required{% endif %} {{ field.attributes|raw }}>
	{% include "/yn/components/includes/calculateDates.twig" %}
{% endblock field %}