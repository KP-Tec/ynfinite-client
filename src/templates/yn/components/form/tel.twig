{% extends "yn/components/form/basic.twig" %}

{% block field %}
	<input type="{{field.type}}" name="fields{{parent}}[{{field.alias}}]" {% if field.pattern %} pattern="{{field.pattern}}" {% endif %} value="{{field.value|default(addValue)}}" placeholder="{{field.placeholder}}" {% if field.required %} required {% endif %} {{field.attributes|raw}}/>
{% endblock field %}
