{% extends "yn/components/form/basic.twig" %}

{% block field %}
	<input aria-label="{{ field.name ?: field.label ?: field.alias }}" id="{{form.alias}}_{{ field.alias }}_{{ field._id }}" data-ynfield="true" id="{{ field.alias }}" type="{{ field.type }}" name="fields{{ parent }}[{{ field.alias }}]{%if field.multiple %}[]{% endif %}" {%if field.multiple %} multiple {% endif %} {%if field.accept %} accept="{{ field.accept }}" {% endif %} {% if field.autocomplete %}autocomplete="{{ field.autocomplete }}"{% endif %} {% if field.required %}required{% endif %} {{ field.attributes|raw }}>
{% endblock field %}
