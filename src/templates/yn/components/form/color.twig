{% extends "yn/components/form/basic.twig" %}

{% block field %}
	<input aria-label="{{ field.name ?: field.label ?: field.alias }}" id="{{form.alias}}_{{ field.alias }}_{{ field._id }}" data-ynfield="true" type="{{ field.type }}" name="fields{{ parent }}[{{ field.alias }}]" value="{{ field.value|default(addValue) }}" placeholder="{{ field.placeholder }}" {% if field.autocomplete %}autocomplete="{{ field.autocomplete }}"{% endif %} {% if field.required %}required{% endif %} {{ field.attributes|raw }}>
{% endblock field %}
