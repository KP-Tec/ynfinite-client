<img src="{{src}}"
    {% if srcset %}
      srcset="{{srcset}}" 
    {% endif %} 
    {% if image.metadata.title %}
      title="{{image.metadata.title}}" 
    {% endif %}
    alt="{{image.metadata.alt|default(image.metadata.filename|default(image.name))}}"
/>